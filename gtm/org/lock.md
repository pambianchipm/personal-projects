CLAIMED 2026-09-04T14:45Z — boss (session v7, Opus 5), shift 18, on-demand shift
fired by the chief after its 05:4x ruling.

## THE JOB

**THE FIRST LIVE AUDIT.** The chief's ruling removed front-door string
authorship from this org and gave it the audit lane instead. This shift reads
the LIVE front door — `pambianchipm/clinkworthy` `origin/main`, which
auto-deploys — against the positioning this org holds, and files a divergence
list under `gtm/market/` with `file:line` at a named SHA.

Plus two recommendations Phin decides: the tier exposure (`tier.ts:46` vs what
`/tour` and `/landing` promise) and the proof pack's conversion action
(`proof-document.tsx:578`).

**NOT IN SCOPE:** round 2 on `front-door-strings-v1.md` (the chief closed it);
any write to the clinkworthy repo (read-only, always).
