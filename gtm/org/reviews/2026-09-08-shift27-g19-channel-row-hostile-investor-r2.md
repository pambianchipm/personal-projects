# HOSTILE-INVESTOR GATE — G19, THE CHANNEL ROW (assumptions.md A47) — ROUND 2 (THE LAST ROUND UNDER AS-1)

CONFIDENTIAL — internal-audit class (law 8).

First breath: Player 2 is an AI marketing employee for small businesses; this verdict grades the one truth-table row (A47) that decides what its copy may claim per platform, at the second and last round.

**VERDICT: FAIL (round 2) — SPLIT.** Two MUST-FIX, both landing in the same cell: the *"where the org's other artifacts disagreed"* parenthetical. Round 1's MF-2 and SF-1 are closed by drafts that re-derive from the paste; round 1's MF-1 was applied as three real amendments and then described in the row by a sentence-group that does not re-derive from the file as it stands at the target — two of its three line pointers land on unamended lines, and a fourth live restatement of the superseded grade sits in the same file where the row's own instrument cannot see it. Every grade cell, the grade/status cell and the four limitations re-derive and SHIP under SOURCED; the parenthetical is PULLED under a banner.

**Target:** `gtm/raise/model/assumptions.md`, row A47 only, at `3301fed`. Re-derived by this reviewer:

```
$ date -u                                   # Tue Sep  8 02:47:13 UTC 2026
$ git rev-parse --short HEAD                # 0ab3cc2   (branch claude/b2c-mlp-ideas-gtm-76bs8z)
$ git show 3301fed:gtm/raise/model/assumptions.md | grep '^| A47' | sha256sum
f804e6feae05c80269ac39a7ee964ea312155ee6a70b3815c416452c9ed49e9b  -
$ grep '^| A47' gtm/raise/model/assumptions.md | sha256sum
f804e6feae05c80269ac39a7ee964ea312155ee6a70b3815c416452c9ed49e9b  -
$ grep -n '^| A47' gtm/raise/model/assumptions.md | cut -c1-4    # 456:
$ grep -c '^| A47' gtm/raise/model/assumptions.md                # 1
$ grep '^| A47' gtm/raise/model/assumptions.md | wc -c           # 6883
```
Both hashes equal the hash `lock.md` and the READY FOR REVIEW entry (02:45Z) print. *Blind spot: `grep '^| A47'` proves the one line is byte-identical at target and HEAD; it says nothing about the legend lines the A47 commits added (`:164`, `:363`) or about `position-v2-the-hire.md`, which are read below, not hashed.*

**FREEZE HONOURED.** `git log --name-only --format='%h %ad %s' --date=iso 3301fed..HEAD` at 02:47:13Z returned three commits, all BEFORE convening (02:45Z): `b22a78b 2026-09-07 23:48:02` (`inbox.md`, `lock.md`, `ops-log.md`), `036e801 2026-09-08 00:46:16` (`inbox.md`), `0ab3cc2 2026-09-08 02:46:12` (`inbox.md`, `lock.md` — the convening commit). `git status --short` was empty. The READY FOR REVIEW entry says *"the two commits between target and HEAD touch `inbox.md`, `lock.md` and `ops-log.md` only"* — exact for the two strictly between (`b22a78b`, `036e801`); the convening commit itself touches `inbox.md` + `lock.md`. The entry follows the target because the chief named an existing commit (inbox 00:46Z, item ii); disclosed in the entry, same shape as the re-cut's round 2. Nothing in the two-file list was breached during the review. *Blind spot: the log proves nothing moved on this branch; it cannot see a working-tree edit made after 02:47:13Z and before the re-derivation at the foot of this file, which is why that block exists.*

**Fan-out: one independent hostile-investor reviewer, gate time only.**

---

## RESULT IN ONE LINE

**All seven grades re-derive from the paste, the X headline now carries the document's qualifier, the Reddit cell is verbatim, the pasted `grep -c` instrument runs as pasted and returns 7 at every commit named — but the row's present-tense pointer *"the `~~` and the dated ⚠ on `:522`, `:1054` and `:1158`"* lands on `### 6.2 Sayable / not sayable` and *"nowhere in this floor…"* at the target (the amendments sit at `:1058` and `:1162–1163`, four lines down, moved by the fix itself), and *"the remaining hits sit inside `>` blockquotes or struck text as history"* is false for `position-v2-the-hire.md:75–76` — *"the Reddit row is now / NOT CHECKED"*, live, unstruck, un-⚠'d, and invisible to `grep -n "Reddit" | grep -c "NOT CHECKED"` because the two strings sit on different lines.**

---

## THE SEVEN GRADES, RE-DERIVED FROM THE PASTE

Instrument: `cat -n gtm/market/snapshots/2026-09-07-channel-readiness-from-clinkworthy.md` at 02:47:30Z; the grade is the last cell of each table row, read by me from the paste, not from A47. The row's cell is the bold headline at the start of A47's value cell.

| Platform | Paste line | The document's grade, in its words | The row's headline | Agree? |
|---|---|---|---|---|
| Instagram | 32 | `**SHIPPED**` | Instagram SHIPPED | YES |
| Facebook | 33 | `**SHIPPED**` | Facebook SHIPPED | YES |
| Meta ads | 34 | `**SHIPPED**` | Meta ads SHIPPED | YES |
| X | 35 (+ 82–83) | `**PARTIAL — verify before claiming**` | X PARTIAL — verify before claiming | YES — the qualifier round 1's SF-1 found dropped is now carried, character for character |
| Reddit | 36 | `**BLOCKED on credentials**` | Reddit BLOCKED on credentials | YES |
| TikTok | 37 (+ 45–46) | `**SHELL ONLY**` | TikTok SHELL ONLY | YES |
| Google Ads | 38 (+ 52–54) | `**LABEL ONLY**` | Google Ads LABEL ONLY | YES |

The document's two summary sentences (paste 62–63, 67–68) name the same three as ✅ and the same two as ❌; the row quotes both (hits below). The paste's own byte claim reproduces at 02:47:30Z: `tail -n +20 <paste> | wc -c` → **4052**; `tail -n +20 <paste> | sha256sum` → **21abaa4cd0a0e30d5aa30594ecd051d5bd94490a9d5bf72f3fb10a758439afdb**; the file is 85 lines. **Condition (b) is met.** *Blind spot: this re-derives the grades from the PASTE and proves the paste's body equals what its own header hashed; it cannot prove that was `origin/main:docs/channel-readiness.md` — no clinkworthy clone on this box, and the brief bars `add_repo`, clone and fetch. Said plainly: the clinkworthy door is shut to this session.*

---

## ROUND-1 ITEMS AS APPLIED

### MF-1 (law 4: three live `NOT CHECKED` Reddit lines in `position-v2-the-hire.md`) — the amendments are real; the row's description of them does not re-derive. See MUST-FIX 1 and 2 below.

**What I ran (02:47:51Z–02:51:43Z), and what it returned:**
```
$ git show 03710e8 -- gtm/market/position-v2-the-hire.md     # three hunks: @@ -519,7 +519,11 @@ (4 lines added at :522–526),
                                                              #   @@ -1051,7 +1055,7 @@ (one line rewritten), @@ -1155,7 +1159,10 @@ (3 lines added)
$ git show 9a814c8:… | wc -l ; git show 3301fed:… | wc -l    # 1167 → 1174
$ diff <(git show 3301fed:gtm/market/position-v2-the-hire.md) gtm/market/position-v2-the-hire.md   # SAME (file unchanged target→HEAD)
$ for n in 522 523 1054 1058 1158 1162 1163; do git show 3301fed:… | sed -n "${n}p"; done
522:  PARTIAL and unverified, ~~**Reddit is NOT CHECKED**~~ **Reddit is BLOCKED on
523:  credentials** *(⚠ AMENDED 2026-09-07, shift 26, at the G19 gate (MF-1, law 4):
1054: ### 6.2 Sayable / not sayable
1058: | "Posts, replies and runs ads on Instagram and Facebook today. …" | … (X PARTIAL, Reddit ~~**NOT CHECKED**~~ **BLOCKED on credentials** — ⚠ amended 2026-09-07 shift 26 to A47, G19 gate MF-1, law 4) |
1158:     nowhere in this floor. A finding buried in a parenthetical is disclosed
1162:     that open (5 of 7 corroborated); ~~**the Reddit grade is now NOT CHECKED**~~
1163:     *(⚠ 2026-09-07, shift 26, G19 gate MF-1: **BLOCKED on credentials** per
$ for n in 522 1054 1158; do git show 9a814c8:… | sed -n "${n}p"; done    # at the PRE-fix commit these three WERE the NOT CHECKED lines
$ grep -n "Reddit" gtm/market/position-v2-the-hire.md | grep -c "NOT CHECKED"     # 7   (the row's instrument, as pasted)
$ git show 9a814c8:… | grep -n "Reddit" | grep -c "NOT CHECKED"                    # 7
$ git show 03710e8:… | grep -n "Reddit" | grep -c "NOT CHECKED"                    # 7
$ git show 3301fed:… | grep -n "Reddit" | grep -c "NOT CHECKED"                    # 7
$ (per hit: line, ~~, leading >, ⚠)   475:0/1/0  483:0/1/0  498:0/1/0  509:0/0/0  522:1/0/0  1058:1/0/1  1162:1/0/0
$ awk '/Reddit/{r=NR} /NOT CHECKED/{ if (r==NR-1) print r"-"NR }' gtm/market/position-v2-the-hire.md    # 75-76
$ sed -n 75,76p gtm/market/position-v2-the-hire.md
   exist.** Re-anchored to code receipts that open; the Reddit row is now
   NOT CHECKED.
$ (line 75, 76: ~~ 0, > 0, ⚠ 0)   ;  diff against 9a814c8 → UNCHANGED
$ sed -n 40,80p gtm/market/position-v2-the-hire.md    # :75–76 is item 4 of "What round 3 found that three prior rounds did not:", live header prose
```
**Verdict on the item:** the three amendments the round-1 verdict asked for exist, each with `~~` and a dated ⚠, and the pasted instrument reproduces (7 → 7, at all four commits, as pasted — law 4's amendment is satisfied for that instrument). The A47 sentences describing the state do NOT re-derive: two of three pointers are the pre-fix line numbers, copied from the round-1 verdict after the fix had moved the lines (rule 1 — the correction's own neighbours were not re-read); and the claim that every other hit is history is false at `:75–76` and inexact at `:509`. The boss's re-drafted parenthetical passes rule-2 register (no forbidden word; instrument, timestamp, return and one blind spot named) but names the wrong blind spot: it says a line count cannot see the amendment (true) and does not say a per-line grep cannot see a `Reddit`/`NOT CHECKED` pair split across a line-wrap (the one that hides `:75–76`). *Blind spot of mine: `awk` finds only the adjacent-line pair; a paraphrase ("unverified", "not established") of the regrade in other words would not appear to any grep here. I read `:40–80`, `:460–545`, `:1045–1060`, `:1095–1110`, `:1150–1165` by eye; the other ~1,000 lines were not read line by line.*

### MF-2 (Reddit cell not verbatim) — CLOSED by a re-derivable draft.
Script `scratchpad/qcheck2.py` (02:47:50Z; strips `**`, unescapes `\|`→`|`, collapses whitespace, matches each `*"…"*` in the A47 line against the paste joined into one line, reports hit count and spanned lines): the Reddit cell *"in the `Channel` union; **credentials still on Phin's list** \| — \| — \| — \| **BLOCKED on credentials**"* → **hits=1, paste line 36**. The three em-dash cells round 1 found elided are present. The cell's trailing note names the first draft's elision and the gate that found it — register is fine. *Blind spot: the matcher normalises bold and whitespace, so a quotation differing only in bold placement would pass; applied uniformly to all 24 quotations.*

### SF-1 (X headline dropped the qualifier) — CLOSED. Headline reads `X PARTIAL — verify before claiming`; paste :35's grade cell is `**PARTIAL — verify before claiming**` (table above). Re-derived from the paste, not from round 1's wording.

### SF-3 (`front-door-strings-v1.md:365`) — APPLIED as a pointer. `sed -n 360,372p` (02:47:59Z) shows the bullet at `:365–369` now ends *"*(⚠ 2026-09-07, shift 26: adjudicated — `assumptions.md` A47 is the anchor, BLOCKED on credentials; `position-v2` §6.2 amended the same shift at the G19 gate.)*"*; the file's header (`:3`) is CLOSED — SUPERSEDED. The pointer does not say A47 is GATE-FAILED; a reader takes *"adjudicated"* as settled by a gated row. Folded into SF-1 below, not a fail.

---

## MUST-FIX

### MF-1 — LAW 4 / RULE 1: the row says every other `NOT CHECKED` Reddit hit in `position-v2-the-hire.md` is history; `:75–76` is live, present-tense, unstruck, and the row's pasted instrument is blind to it by construction.

**Sentence at fault (A47, the parenthetical):** *"… the string stays inside `~~…~~` on the three amended lines, so a line count cannot see the amendment; what a reader can see is the `~~` and the dated ⚠ on `:522`, `:1054` and `:1158`, and the remaining hits sit inside `>` blockquotes or struck text as history."*

**What I ran, and what it returned:** the block under MF-1 as applied, above. In short: the instrument's seven hits classify as three inside `>` (`:475`, `:483`, `:498`), three struck (`:522`, `:1058`, `:1162`), and one — `:509` — neither (it is the amended table row, live, stating BLOCKED and naming `NOT CHECKED` as the prior grade). Outside the instrument's sight, `:75–76` reads *"`channel-readiness.md`, cited as "the authority" for §3.1, does not exist. Re-anchored to code receipts that open; the Reddit row is now NOT CHECKED."* — no `~~`, no `>`, no ⚠, byte-identical to `9a814c8`, in the same file the boss amended in `03710e8`. `grep -n "Reddit" … | grep -c "NOT CHECKED"` cannot return it because *Reddit* is on `:75` and *NOT CHECKED* on `:76`.

**Why a gate-fail:** law 4 — A47 states Reddit `BLOCKED on credentials`; `position-v2-the-hire.md:76` states `NOT CHECKED` in the present tense, in live header prose, exactly the shape round 1 failed at `:1158` (*"LIVE — floor item 20, present tense"*) and the boss struck. It is the fourth site of the claim the boss's own lock handoff says to sweep for *"in every wording you have used for it"*. And law 4's corollary, applied a second time to the same row: the instrument that found seven hits is not evidence the search was complete — the row states the count, states one blind spot of the count, and does not state the blind spot that matters. Rule 3: a result without its instrument's limitation is a claim. **Both fail:** A47 for the sentence, `position-v2-the-hire.md` for `:75–76` (and the same item's *"does not exist"*, which `:1163`'s ⚠ already contradicts for floor item 20).

**What it should say (a new draft, re-derived from the file after the fix, not from this verdict):** name the instrument's blind spot in the same sentence — e.g. *"… (`grep -n "Reddit" … | grep -c "NOT CHECKED"` → 7 before and 7 after; it counts lines carrying both strings, so it cannot see the amendment inside `~~` nor a pair split across a line-wrap — `:75–76`, the header's round-3 item 4, still read *"the Reddit row is now NOT CHECKED"* until amended in `<fix commit>`); of the seven, three sit inside `>` blockquotes (`:475`, `:483`, `:498`), three are struck with a dated ⚠ (`:522`, `:1058`, `:1162`), and `:509` is the amended table row naming the prior grade"*. And strike `:75–76` in `position-v2-the-hire.md` with a dated ⚠ pointing at A47 — a new draft, rule 1.

**WHICH CELL:** the *"where the org's other artifacts disagreed"* parenthetical. **PULLED.**

*Blind spot of my instrument: the `awk` adjacent-line test catches a two-line split only; a three-line wrap or a paraphrase would escape it. My by-eye windows are listed above; the file is 1,174 lines.*

### MF-2 — CITATION DOES NOT RESOLVE: two of the row's three line pointers name unamended lines at the target.

**Sentence at fault (A47, the same parenthetical):** *"what a reader can see is the `~~` and the dated ⚠ on `:522`, `:1054` and `:1158`"* — present tense, about the file as it is. (The earlier clause *"…at `:1054` and floor item 20 at `:1158` still read `NOT CHECKED` when this row was first written"* is past tense and TRUE of `9a814c8`, where those were the lines; it is not at fault.)

**What I ran, and what it returned (02:50:11Z):** `git show 3301fed:gtm/market/position-v2-the-hire.md | sed -n '1054p;1158p'` → `### 6.2 Sayable / not sayable` and `    nowhere in this floor. A finding buried in a parenthetical is disclosed` — neither carries `~~` or ⚠. The amended §6.2 column is `:1058`; the amended floor item 20 is `:1162` with its ⚠ at `:1163`. Cause, from `git show 03710e8`: the fix inserted four lines at `:522–526`, shifting everything below by four; the boss then wrote the round-1 verdict's pre-fix numbers into the row (`03710e8`) and repeated them in `3301fed` and in the 02:45Z READY FOR REVIEW entry (*"`:522`, `:1054`, `:1158`"*).

**Why a gate-fail:** condition (a) and law 2 — a citation is a line a reader can open. A hostile reader following `:1054` finds a heading and `:1158` finds a sentence about parentheticals, and concludes the amendment was claimed, not made. Round 1 passed *"every line number is right"* as a condition of the row; at this target two of the three that describe the fix are wrong. This is rule 1 exactly: the correction was applied as the verdict's wording and its own neighbours — the numbers the correction itself moved — were not re-read against the file.

**What it should say:** `:522`, `:1058`, `:1162–1163` — re-read from `cat -n` of the file at the fix commit, and, since `front-door-strings-v1.md:371` already says it for that file, either pin the numbers to a commit (*"at `03710e8`"*) or say they drift.

**WHICH CELL:** the *"where the org's other artifacts disagreed"* parenthetical — the same cell as MF-1. **PULLED.**

*Blind spot: `sed -n` proves what is on a line; it cannot tell me what the author meant to point at — I inferred the intended lines from the `03710e8` diff hunks.*

---

## SHOULD-FIX

- **SF-1 — A47's status as restated OUTSIDE the row is stale in four places.** `assumptions.md:164` and `:363` (identical by `diff`, 02:48:09Z) end *"A47 is UNGATED until its own gate"*; `position-v2-the-hire.md:1108` says *"(⚠ A47 written 2026-09-07, shift 26, UNGATED)"*; `gap-list-2026-09-04.md:548`, `:585` carry the same legend sentence; `front-door-strings-v1.md:367` says *"adjudicated"*. At `3301fed` the row's own cell says **GATE-FAILED (round 1)** — its gate has convened and failed it. "Ungated" (not GATED) is still true; "until its own gate" is not. None is a platform grade and none is inside the A47 line, so not a fail; but a reader of the legend is told the gate is ahead when it is behind. Re-draft each to the row's status at the fix commit (and, after this verdict, to SPLIT).
- **SF-2 (round 1's SF-2, still open at this target — NOT a fail on its own, said explicitly):** `grep -rn "d4ae10d" gtm --include=*.md | grep "21abaa4c"` (02:50:18Z) → two lines, the round-1 verdict's SF-2 bullet and the A47 line itself, where the two strings are 2,000 characters apart in different sentences. The chief printed `git show d4ae10d:docs/channel-readiness.md | sha256sum` → `21abaa4c…`, `wc -c` → 4052, at 00:46Z (inbox); it is not yet in limitation (1) because fixes land after this verdict as a new target. The join is still by byte count in the row's text. Write the chief's line in, pasted as run, dated.
- **SF-3 — `position-v2-the-hire.md:526–527`, a neighbour the MF-1 fix left dangling.** After the ⚠ the sentence continues *"(regraded NOT CHECKED at the round-3 gate; round 4 found the regrade had reached the narrative and not the table);"* — a provenance clause that belonged to the struck words and now hangs off *"BLOCKED on credentials"*, so the sentence reads *Reddit is BLOCKED on credentials (regraded NOT CHECKED…)*. Past tense, so not a live grade — but it is the row's instrument's blind spot again (`:526` carries `NOT CHECKED` without `Reddit`). Strike or move it. Lands in `position-v2`, not in the row.
- **SF-4 — `:509` in the row's "remaining hits" sentence.** Covered under MF-1; listed so the re-draft accounts for all seven, not six.
- **SF-5 — round 1's SF-4, SF-5, SF-6, SF-7 are unapplied and the row says so** (*"SF-2, SF-4–SF-7 not yet applied"*). Re-checked at this target: `risk-register.md:29`, `skeleton.md:252`, `ksp-funnel.md:224–227`, `objection-bank.md:184` still state the secondhand-signals / *"reads Reddit firsthand"* claims with no scope clause in A47 (02:50:25Z); the legend fragment is still unmarked as a fragment (verbatim between its ends: `sed 's/\*\*//g' assumptions.md | grep -c -F <fragment>` → 1, 02:50:18Z); `position-v2:497` still says *"X publish ships"* inside the frame. Not the document's four columns, so not law 4; carried as round 1 filed them.

A SHOULD-FIX splits nothing.

---

## WHAT PASSED, WITH THE INSTRUMENT

- **Twenty-one of twenty-four quotations verbatim at the stated lines, each found exactly once** (`qcheck2.py`, 02:47:50Z): 62–63; 67–68; 32; 33; 34; 35; 82–83; 36 (full cell); 37; 45–46 (×2 uses); 38; 52–54; 26–27; 28; 81; 81–82; 84; 74–75; 85; 36 (`BLOCKED on credentials` alone). The three with hits=0 are, by the row's own attribution, not from the paste: the legend fragment (verified against `assumptions.md:365` as above); *"IG/TikTok signals secondhand"* (`grep -rn` → `skeleton.md:252`, `risk-register.md:29`, verbatim); the `SHELL_PLATFORMS` comment (`gap-list:600`, `corpus-grade.md:427` carry it; not re-derivable at source here). The row's *"(line 80–85)"* and *"(line 73–78)"* section pointers match `cat -n`.
- **The pasted instrument runs as pasted and returns what the row says** — 7 at `9a814c8`, `03710e8`, `3301fed`, HEAD (02:47:58Z). Law 4's amendment is satisfied for the output; the instrument's unstated blind spot is MF-1.
- **The row's provenance facts match the paste's header:** `3e24d43d…` (paste :11), `5f0de311… 2026-09-03T19:53:52+00:00` (paste :13), retrieved 23:16Z (paste :3, :6), four commands (paste :6–:13).
- **The grade/status cell is exact at the target:** GATE-FAILED (round 1), verdict path correct, *"two MUST-FIX, seven SHOULD-FIX"* matches the r1 file's foot, *"MF-1, MF-2 and SF-1/SF-3 applied as NEW DRAFTS in the commit that files the verdict"* matches `git log --name-only 9a814c8..3301fed` (`03710e8` touches the verdict, `assumptions.md`, `position-v2`, `front-door-strings`, `gap-list`; `3301fed` touches `assumptions.md` alone, one line). *"Round 2 is NOT convened this shift … target = the commit carrying this line"* — the chief named `3301fed`, the commit carrying the line.
- **The limitations each carry an attempt or a source sentence:** (1) `add_repo` 23:23Z → denied — now on the record at `ops-log.md:59–60` and in the lock handoff (read 02:50:20Z; a record, not the event); `git clone` not re-tried, pointing at shift 25's record (`ops-log.md:338`: *"It was `add_repo` + one `git clone`"*). (2) paste :28, hits=1. (3) paste :81, :81–82, :84, :74–75, :85, each hits=1. (4) `gap-list:599–602` and `corpus-grade.md:426–429` both carry the tension in the row's words. SF-2's hash line is absent — open, not a fail, per the chief's sequencing.
- **Rule-2 register on the A47 line clean** (`grep -o -i "fixed\|checked\|only\|now true\|verified\|confirmed\|closed"`, 02:48:09Z): `CHECKED ×4` (all `NOT CHECKED` — the document's section, and three about `position-v2`'s regrade), `ONLY ×4` (`SHELL ONLY` ×2, `LABEL ONLY` ×2). None about the author's own work. *"Condition (b) held"* is attributed to the reviewer and re-derived here.
- **Law 5 and law 8:** `assumptions.md:3` — `CONFIDENTIAL — internal-audit class (law 8).`; first breath at `:400` (*"Player 2 is an AI marketing employee for small…"*), where round 1's SF-7 noted it. Unchanged; the chief's.
- **Law-4 sweep otherwise clean** (`grep -rn -i "tiktok\|google ads\|reddit\|shell only\|label only\|blocked on credentials\|partial" gtm/market gtm/raise --include=*.md`, 02:48:02Z: 151 hits, 28 files; every hit stating a Player 2 platform grade opened in context at 02:50:25Z): `live-front-door-audit-2026-09-04.md:397–403`, `:739–742`; `position-v2-framing-v1.md:224`; `front-door-overhaul-receipts.md:431`; `front-door-strings-v1.md:321–324`, `:365–369`; `position-v2-the-hire.md:504–511`, `:522–523`, `:1058`, `:1162–1163` — all state the document's grades and agree with the row. `positioning.md:53` is Adobe's *"on TikTok"* (a survey population, not a grade). The one disagreement found is `position-v2-the-hire.md:75–76` — MF-1. *Blind spot: `grep -i` on seven terms; an artifact that states a grade in other words ("can post to", "connected") would not be in the 151.*

---

## NOT CHECKED BY THIS VERDICT — the attempt behind each

- **The paste's four provenance commands and the `d4ae10d` hash the chief printed at 00:46Z.** Attempt: none possible — `ls /home/user` at 02:54:36Z (the final block below) returned only `personal-projects`; the brief bars `add_repo`, clone, fetch. Established instead: the paste's body from line 20 hashes to `21abaa4c…` at 4,052 B. Whether `3e24d43d` was GitHub `main`'s tip at 23:16Z is not knowable here.
- **The document's sixteen code receipts and the `SHELL_PLATFORMS` comment at any commit.** Same bar. The row rests on the r2 verdict's *"sixteen of sixteen"*, which I read and did not re-run.
- **The `add_repo` denial at 23:23Z as an event.** Attempt: `grep -n "23:23\|add_repo" gtm/org/ops-log.md` → `:59–60` records it. A record written by the boss; not witnessed.
- **`position-v2-the-hire.md` beyond the windows named under MF-1.** Read: `:40–80`, `:460–545`, `:1045–1060`, `:1095–1110`, `:1150–1165`, plus every hit of the sweep terms and of `NOT CHECKED`. A paraphrase outside those windows would not have been seen.
- **`git clone` from this session.** Not attempted (barred), so the row's *"was not re-tried"* is neither confirmed nor contradicted here.

**Instrument of this verdict:** `git show`/`git log`/`git rev-parse`/`git status`/`git diff` on the frozen tree; `cat -n`, `sed -n`, `tail -n +20`, `wc`, `sha256sum`, `diff`, `awk` on the paste and on `position-v2-the-hire.md`; a Python quotation matcher (`scratchpad/qcheck2.py`) over the A47 line against the paste; `grep -rn -i` over `gtm/market` and `gtm/raise`; direct reads of the doctrine, `boss.md`, the two newest inbox entries, the round-1 verdict, the lock, the legend, G19's status, the parent receipt's §5 tension. Every timestamp is a `date -u` printed at the head of the command batch that produced the line. No external URL fetched, no repo added, no MCP tool called, no tracked file edited; the only file created in the tree is this one.

**THE SPLIT, STATED PLAINLY (the chief's ruling, 00:46Z, item ii):** SHIP under SOURCED — the Instagram, Facebook, Meta ads, X, Reddit, TikTok and Google Ads cells; the grade/status cell; limitations (1)–(4) (with SF-2 open on their face). PULLED under a banner — the *"Where the org's other artifacts disagreed"* parenthetical, from *"`position-v2-the-hire.md` regraded Reddit…"* through *"…carries a pointer here"*, until a new draft re-derives it from `position-v2-the-hire.md` after `:75–76` is amended. The sentence *"This row is the anchor: the document's sentence governs"* and the blockers sentence after it re-derive (paste :73–78) and ship.

---

## FINAL RE-DERIVATION
```
$ date -u
Tue Sep  8 02:54:36 UTC 2026
$ ls /home/user
personal-projects
$ git rev-parse --short 3301fed; git rev-parse --short HEAD
3301fed
0ab3cc2
$ git show 3301fed:gtm/raise/model/assumptions.md | grep '^| A47' | sha256sum
f804e6feae05c80269ac39a7ee964ea312155ee6a70b3815c416452c9ed49e9b  -
$ grep '^| A47' gtm/raise/model/assumptions.md | sha256sum
f804e6feae05c80269ac39a7ee964ea312155ee6a70b3815c416452c9ed49e9b  -
$ tail -n +20 gtm/market/snapshots/2026-09-07-channel-readiness-from-clinkworthy.md | sha256sum
21abaa4cd0a0e30d5aa30594ecd051d5bd94490a9d5bf72f3fb10a758439afdb  -
$ git log --name-only --format='%h %ad %s' --date=iso 3301fed..HEAD
0ab3cc2 2026-09-08 02:46:12 +0000 gtm shift 27: lock + READY FOR REVIEW — round 2 (last under AS-1) convened on A47 (G19), target frozen at 3301fed as the chief named it

gtm/org/inbox.md
gtm/org/lock.md
036e801 2026-09-08 00:46:16 +0000 gtm inbox: shift 26's three asks ruled — split confirmed, A47 round 2 first, SF-2 hash pasted as run

gtm/org/inbox.md
b22a78b 2026-09-07 23:48:02 +0000 gtm shift 26 CLOSE: ops-log with fan-out disclosure (two reviewers, one per gate), inbox close notice with NOT CHECKED and three asks for the chief, chief's 23:17Z ruling flipped DONE, lock released with handoff

gtm/org/inbox.md
gtm/org/lock.md
gtm/org/ops-log.md
$ git status --short   (expected: only this verdict file, untracked)
?? gtm/org/reviews/2026-09-08-shift27-g19-channel-row-hostile-investor-r2.md
```

Target unchanged before and after this review; the only tree difference is this file. **FAIL (round 2) — SPLIT: 2 MUST-FIX (both in the "where other artifacts disagreed" parenthetical, PULLED), 5 SHOULD-FIX; seven grade cells, the grade/status cell and limitations (1)–(4) SHIP under SOURCED.**
