# Boss disposition — `front-door-strings-v1.md` round 1

GTM boss, shift 17, 2026-09-04. Written **after** both verdicts were filed
verbatim and **after** every proposed remedy was read against source and
against `position-v2-the-hire.md` §6.2, per `boss.md` rule 1.

BOTH GATES FAILED THE ARTIFACT. It is `GATE-FAILED (round 1)` with edits
applied and unverified.

---

## THE FINDING OF THIS ROUND, and it is the fifth occurrence of one shape

**A GATE'S PROPOSED REMEDY RESTORED THE UNQUALIFIED APPROVAL CLAIM AGAIN.**
The SMB owner's MUST-FIX 4 replacement band opens *"Nothing posts and nothing
spends without your click."* as a standalone sentence — `position-v2` §6.2's
PROHIBITED row, and `PRINCIPLE` (`console/app/landing/content.ts:184`) in
different words. Round 3 of the previous artifact produced the same shape from
a different persona. **Five rounds, five authors, one shape**, and this is the
second consecutive round where it arrived inside a proposed *fix*.

**AND THIS ROUND IT ALSO CAUGHT THE BOSS.** The standing recommendation from
shift 16 — `YOUR NEXT HIRE · JOINING SOON` — was killed by both gates
independently, neither knowing the other's finding, for the same reason:
*"soon"* asserts a timeframe against the PHIN-APPROVED
`waitlist-front-door.md:26`, *"No date, because we don't have one."* **The
boss's own remedy for the largest defect on the page carried a defect.** It is
withdrawn and replaced with the verifier's `YOUR NEXT HIRE · NOT BUILT YET`,
built from the approved words at `waitlist-front-door.md:22`.

**The rule that produced both catches is `boss.md` rule 1**, and it now has a
symmetric record: it caught a gate in round 3 of the last artifact, and it
caught the author in round 1 of this one.

## MUST-FIX — FRESH VERIFIER

| # | Finding | Disposition |
|---|---|---|
| 1 | `waitlist-front-door.md:74-76` cited for the zero-owners claim; that range is item (ii), *"the hero is canon and locked… a spec change for the build org"* | **TAKEN.** Repointed to `:71-73`. `:74-76` is now a header block in the artifact stating that §1 and §4 are spec-change requests routed through the chief, not copy edits. **Read at source before applying**: `:71-73` is item (i), `:74-76` is item (ii) |
| 2 | Three live unqualified approval claims (`content.ts:87`, `:167`, `:178`) in no Replaces table; the file said the claim appears "once" | **TAKEN, with one correction to the remedy.** The remedy put all three in §4's Replaces table. `:87` is the live **hero sub**, so it belongs in §1's table and is there. `:167` and `:178` are `BUILT_FOR` card bodies and are in §4's. The word "once" is gone; the grep and its seven-line output are printed in its place, with the instrument's blind spot named in the same paragraph |
| 3 | The hero trips §6.2's availability row; the file's own remedy asserts a timeframe | **TAKEN.** `JOINING SOON` withdrawn; `NOT BUILT YET` recorded as the recommendation. **Not applied — it edits Phin's ruled line and stays Phin's** |
| 4 | `WAITLIST_ONLY` is default-ON; `POST /api/scan/checkout` 403s | **TAKEN.** New OPEN 5 in §5, citing `waitlist-gate.ts:32-35`, `:12`, `checkout/route.ts:37-38`, `landing/page.tsx:190`. Found independently by the boss before the verdict landed, from the same read. **NOT CHECKED: the production value of the flag** — this org cannot read it |
| 5 | "runs your Meta ads" and the advance clause are `os` entitlements receipted against a **platform** grade | **TAKEN.** `tier.ts:46`, `:53`, `:79` added to both receipt rows; OPEN 3 and OPEN 7 raised. **This is the best finding in either verdict** — the artifact had opened `tier.ts:107` and `:109` and not `:46`, nine lines up |
| 6 | Footer tagline is a reordered variant of the ruled string, cited with no line number | **TAKEN.** The ruled string ships byte-for-byte, cited `MARKETING.md:133`. The verifier's own caveat about `:152` was checked: `:152` overrides *"COPY v2's pricing block and every self-serve CTA"*, which does not reach `:133` |
| 7 | Meta description says "Not built yet" of the whole product | **TAKEN.** Scoped to *"The employee isn't built yet"*. **The arithmetic the verifier said it had not recomputed was recomputed**: the string is now **154 characters** and the qualifier closes at **92**, so nothing truncates at typical widths — but the "~155" width figure is withdrawn as uncited (its SHOULD-FIX 2) rather than leaned on |
| 8 | The illustrative quotation has no receipt | **PARTLY DECLINED.** The quotation stays. It is now labelled `UNTESTED` and ILLUSTRATIVE in the receipt table, with the read-back half receipted and the message-arrival half stated to rest on nothing that ships. Recorded in the artifact's §7 |

## SHOULD-FIX — FRESH VERIFIER

1 TAKEN (the 11pm header's presupposition is now stated; the persona's reaction
is labelled as a persona's). 2 TAKEN (the "~155" figure is withdrawn; the row
carries measured numbers). 3 TAKEN (both §6.2 rows the band must clear are
named, including the Meta-ads/armed-in-advance pair, with how the band breaks
it). 4 TAKEN AS AN OPEN, not applied — the refund is sayable under §6.2 and the
strings carry no price to put it beside; raised to the chief. 5 TAKEN — the
approved clause is **HELD**, not DELETED. 6 TAKEN — §5 no longer names the two
barred channels; it cites their grades by line. It also records that
`channel-readiness.md:17` and §6.2 disagree on Reddit and does not adjudicate.
7 TAKEN — the §1.6 citation now carries a line number
(`position-v2-the-hire.md:227`) and is labelled the weakest receipt in the file.
8 TAKEN — the dropped *"today"* and *"TikTok and Google Ads are coming"* are
disclosed in §1.

## MUST-FIX — SMB OWNER

| # | Finding | Disposition |
|---|---|---|
| 1 | The hero says available; the correction is a band lower in small type | **TAKEN as a finding**, and it corroborates the verifier by a different route. His `STILL BEING BUILT` is recorded next to the verifier's `NOT BUILT YET`. **Phin's** |
| 2 | "moved units" is the most persuasive clause and the one the product cannot do alone | **TAKEN as a finding.** His proposed replacement is recorded verbatim in OPEN 2. **Not applied — Phin's ruled sub** |
| 3 | The form asks for an email and answers nothing | **TAKEN. His remedy DECLINED**: *"We'll open invites here — check back"* asserts an on-site invite mechanism nothing in the code establishes. **The shipped confirmation string is used instead** — `content.ts:247`, which promises an invite without naming a channel |
| 4 | The band could not be parsed on one reading | **TAKEN. His remedy DECLINED** — see the finding at the top of this file. The band is rebuilt so the choice comes first and no clause takes back what an earlier clause gave |
| 5 | No price anywhere | **TAKEN as a finding, DECLINED as an edit.** The live price string is barred twice by §6.2, and OPEN 5 records that checkout refuses by default. **Raised to Phin** |

## SHOULD-FIX — SMB OWNER

1 TAKEN AS AN OPEN — he and the boss recommend cutting **different** halves of
the duplicated four verbs; both recommendations are recorded and the split is
the chief's. 2 DECLINED — naming a chat app is barred at
`waitlist-front-door.md:37-39`. 3 TAKEN — the proof strip now says the scan
needs a website and an email, receipted to `scan/page.tsx:337`. 4 NOT APPLIED —
*"We're not taking money for it"* is PHIN-APPROVED; his objection is recorded.
5 DECLINED — §6.2 licenses a version, the chief's brief for this shift bars
completeness claims about channels; the two disagree and this org did not pick.
6 DECLINED — three artifacts disagree on the count (R6). 7 TAKEN AS AN OPEN —
*"Watch it work a week"* reads to him as his own shop; `tour/page.tsx:10` says
it is a walkthrough. 8 recorded as corroboration of MUST-FIX 1, not a separate
item. 9 no action — he asked for a line to be left alone.

## WHAT THE BOSS FOUND THAT NEITHER GATE DID

- **Item 22 has merged.** The previous artifact's R7 rested on the waitlist
  being a spec. It is shipped code (`landing/page.tsx:192`,
  `content.ts:217-259`), so §2 replaces **live** strings, and the three
  defensive clauses the chief's directive named are on the internet now.
- `console/app/api/waitlist/route.ts:31-34` — *"Nothing here sends email"* —
  in the shipped route that receives the form.
- **PR #98** writes a COPY v4 that bans *"Join the waitlist"* as a dominant
  action while item 22 ships it as the live form CTA. Not resolved here.

## NOT CHECKED IN THIS DISPOSITION

- `npm test` — neither the boss nor either gate executed it. The clone has no
  dependencies. `landing-content.test.ts:73-86` and the `slopCheck` at `:62-71`
  are unrun against every string in the artifact and against every replacement
  proposed in either verdict.
- **No owner has read any string in the artifact, or any remedy in either
  verdict.** Both gates were personas.
- The verifier's blind spot, in its own words: it read windows around each
  cited range, so a citation landing on plausible text a few lines from its
  intended item passes that instrument. It caught the one it caught because
  `waitlist-front-door.md` is short enough to read whole.
- Whether the round-1 edits close what the round-1 verdicts opened. **Nobody
  has re-read them.**
